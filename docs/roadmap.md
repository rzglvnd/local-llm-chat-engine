# Roadmap

- v0.1 (completed): Core API + CLI + tests
- v0.2 (completed): Docker support + retrieval backends + streaming
- v0.3 (completed): Snapshot persistence + readiness + API-key guard

Next milestones

- v0.4: Async inference adapters and background ingest jobs
- v0.5: Metrics export (Prometheus) and tracing integration
- v0.6: Pluggable storage abstraction (Postgres/Redis/vector DB)
- v1.0: Multi-tenant authz, rate limiting, SLO runbooks
